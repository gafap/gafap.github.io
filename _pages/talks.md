---
layout: page
permalink: /talks/
title: Talks
description:
# UNLISTED PAGE. Reachable by URL, but deliberately not advertised:
#   nav: false      keeps it out of the navbar
#   sitemap: false  keeps it out of /sitemap.xml (jekyll-sitemap reads this)
# and _layouts/default.liquid adds a noindex meta tag for any page with
# `noindex: true` below.
#
# This is OBSCURITY, NOT PRIVACY. The repo at github.com/gafap/gafap.github.io is
# public, so anything committed here is browsable in the repo whether or not a
# page links to it. Fine for conference slides you are happy for anyone to read
# but do not want in the navbar. NOT a place for exam papers, solutions, or
# anything you are not licensed to redistribute.
#
# The permalink is a guessable word. If a particular talk ever needs to be less
# findable than that, change `permalink:` to something unguessable rather than
# relying on this page being unlinked.
nav: false
sitemap: false
noindex: true
---

<!-- Add a talk by copying a line below. Put the PDF in `assets/pdf/` and link it
     as `/assets/pdf/<filename>.pdf`. Newest first.

     No HTML and no styling here on purpose: this is an ordinary markdown list, so
     it inherits the site's body type and needs no CSS of its own (CLAUDE.md §6).

     For a talk about a paper that is already on the Research page, there is a
     better home than this page: add `slides = {my-talk.pdf}` to that paper's entry
     in _bibliography/papers.bib and it gets a "Slides" button on its own row. Use
     this page for talks with no paper attached, or when you do not want the link
     public. -->

- **Educare.** EALE, Barcelona 2026.
  [Slides](/assets/pdf/eale_slides_15min.pdf)

<!-- Example of the shape to copy:

- **Low staffing in the maternity ward.** American-European Health Economics Study
  Group, Oxford, September 2025. [Slides](/assets/pdf/aehesg-2025-maternity.pdf)

-->
